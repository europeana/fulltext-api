apiVersion: apps/v1
kind: Deployment
metadata:
  name: fulltext-api-deployment
spec:
  template:
    spec:
      containers:
        - name: fulltext-api
          # required to use local image
          imagePullPolicy: Never